This PR mounts the rest of the page instead of leaving finished components unused. Carlos Pelazas still opens at the top. Selected projects keep the warm brown palette, with DrawToCloud and RenderPR on the cards. How I build is new: parallel coding agents, a multiplexer, and speech-to-text. Experience was already written and never rendered; it now shows the Red Bull intern role and Vivra. Education follows with the AWS Solutions Architect cert. Contact is a real form at the bottom. Header, projects, the new build section, work history, school, then a way to write in.
